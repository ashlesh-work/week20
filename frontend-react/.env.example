# Copy to .env.local and set your Flowise Prediction API endpoint.
# .env.local is gitignored — this file (.env.example) holds NO real values.
# NOTE: this is only the chatflow endpoint URL, never an API key.
VITE_FLOWISE_ENDPOINT=http://localhost:3000/api/v1/prediction/YOUR-CHATFLOW-ID
